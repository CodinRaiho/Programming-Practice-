#include <stdio.h>

int main() {
    int a = 1;  // Initialize a counter variable

    while (a) {  // Repeat while count is less than 10
        printf("8\n");  // Print the number
        a++;  // Increment the counter
    }

    return 0;
}
